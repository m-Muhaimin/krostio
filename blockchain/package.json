{
  "name": "income-verifier-contracts",
  "version": "1.0.0",
  "description": "Krost on-chain attestation contracts",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:base-sepolia": "npx hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:base": "npx hardhat run scripts/deploy.js --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
